# Ignore everything except what we want to publish
*

# Include only core (all contracts live under core/) and abi
!core/**
!abi/**
!README.md
!package.json
!package-lock.json

# Ignore scripts and other dev files (these come AFTER negations)
scripts/
*.cjs
*.js
*.ts
