# Protocol README assets

| File | Purpose |
|------|---------|
| `hero-composition-framework.svg` | README hero — composition diagram (logo slot top-left) |
| `badge-audited-nethermind.svg` | Optional audit strip (sponsor legal review before use) |

## Logo (pending)

When the Bloxchain brand logo is ready:

1. Add `bloxchain-logo.svg` (or `.png`) to this folder.
2. In `README.md`, uncomment the logo `<img>` above the hero, **or** replace the dashed **logo** slot in `hero-composition-framework.svg` with an embedded `<image href="bloxchain-logo.svg" …/>`.
3. Update `.github/social-preview.png` for GitHub link previews (1280×640 recommended).
