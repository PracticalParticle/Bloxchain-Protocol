{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "packages": {
    "package": {
      "release-type": "node",
      "package-name": "@bloxchain/contracts",
      "changelog-path": "CHANGELOG.md"
    },
    "sdk/typescript": {
      "release-type": "node",
      "package-name": "@bloxchain/sdk",
      "changelog-path": "CHANGELOG.md"
    }
  },
  "$comment": "Independent semver per publishable package. Root package.json is not release-managed. Manifest keys are package dirs; changelog-path is relative to each dir (resolves to package/CHANGELOG.md, sdk/typescript/CHANGELOG.md)."
}
